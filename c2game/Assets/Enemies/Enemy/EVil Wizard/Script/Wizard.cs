using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Wizard : EnemyGeneral
{
    
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
        hp = 10;
    }

    // Update is called once per frame
    void Update()
    {
        base.Update();
    }

    
}
